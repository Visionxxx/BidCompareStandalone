# Port Configuration
# Change these if the default ports are already in use

# Frontend port (accessible at http://localhost:FRONTEND_PORT)
FRONTEND_PORT=80

# Backend API port (accessible at http://localhost:BACKEND_PORT)
BACKEND_PORT=8000
